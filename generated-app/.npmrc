# engines in package.json are a constraint, not a suggestion: npm warns by default, and a warning
# scrolls past. Node 24 breaks this project's test run (jsdom's AbortSignal vs. undici's native
# fetch), so an unsupported version must fail the install rather than surface later as four
# confusing test failures. See "Known limitations" in README.md.
engine-strict=true
